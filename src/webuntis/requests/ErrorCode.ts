/** JSON-RPC Error codes returned by the WebUntis API. */
export enum ErrorCode {
  TooManyResults = -6003,
  InvalidCredentials = -8504,
  NoAccess = -8509,
  NotAuthenticated = -8520,
  UserBlocked = -8998,
}
